// Game configuration values

// Audio settings
export const DEFAULT_VOLUME = 30;
export const DEFAULT_MUTED = true;

// Display settings
export const DEFAULT_EMOJI_SIZE = 'small'; // small, medium, large